# EXP-401 (The Blend) — Honest Fill Model Re-run (FIX #3): NO FAITHFUL TWIN

**Date:** 2026-07-10 · **Author:** cc1 (fleet job)
**Code:** main @ post-`11f140c` (FIX #3) · **Runner (proxy only):** `experiments/honest-fills-fleet/run.py exp401core {naive|marketable}`
**Results files:** `experiments/honest-fills-fleet/results/exp401core_{naive,marketable}.json`

## Verdict first: EXP-401 has no faithful backtest twin

Registry: `backtest_config: null`. The deployed config (`configs/paper_exp401.yaml`) is the champion CS+IC core **plus two differentiators, neither of which the engine (`backtest/backtester.py`) can represent**:

1. **`straddle_strangle` overlay** — short straddle/strangle gated to post-FOMC/CPI IV-crush events (`mode: short_post_event`, 5 DTE, 4 % OTM, 3 % risk, event-type gating, IV-crush assumptions). The engine has no straddle/strangle strategy, no event calendar, and no IV-crush model. *(Broker-record note: in the observed Apr–Jul 2026 live quarter, EXP-401 filled 26/26 SPY bull-put spreads and zero straddles — the overlay never fired live either, so its historical contribution is untested everywhere.)*
2. **`regime_scale_*` risk multipliers** — bull 1.0 / bear 0.3 / high_vol 0.3 / low_vol 0.8 / **crash 0.0**, plus separate `ss_regime_scale_*` for the overlay. These are material risk-halving/zeroing features (in a 2020-window backtest, `crash: 0.0` alone would change everything) and the engine has no regime-scaled sizing hook.

Also unrepresented (shared with the whole fleet): the live compass VIX-percentile regime proxy, and the `technical` trend-filter block. **Building these into the engine is what's missing before EXP-401 can have a real twin: (a) an event-calendar + short-vol-event strategy module, (b) regime-scaled risk sizing in the engine's position sizer, (c) compass-proxy parity.** Until then, any "EXP-401 backtest" is fabrication; this file deliberately does not present one.

## What WAS run: the shared champion core, as a labeled proxy

The engine-representable remainder of `paper_exp401.yaml` (CS+IC, DTE 15–25, 2 % OTM, 12-wide, PT 55, SL 1.25×, 17 % flat, max_positions 12) differs from the EXP-400 twin **only** in `max_positions` 12 vs 10 — a cap that never binds in these runs. The proxy runs are therefore **numerically identical to EXP-400's** (verified: metrics dicts equal for both fill models). That identity is itself the finding: *stripped of its unrepresentable differentiators, EXP-401 is EXP-400.* See `reports/honest_fills_fleet/EXP-400.md` for the full analysis, including the drawdown-breaker path-dependence caveat that dominates both runs.

| Metric (proxy = champion core, NOT EXP-401) | naive | marketable |
|---|---|---|
| Trades | 28 | 83 |
| Total return | −60.97 % | −74.53 % |
| Win rate | 53.57 % | 63.86 % |
| Sharpe | −0.68 | −0.48 |
| Max DD | −61.77 % | −80.67 % |
| Unfilled slot attempts | 0 | 11,076 |

## Machine-readable results

```json
{
  "experiment": "EXP-401",
  "twin_status": "NO_FAITHFUL_TWIN",
  "twin_faithful": false,
  "missing_for_faithful_twin": [
    "straddle_strangle short_post_event overlay (engine has no straddle strategy, no FOMC/CPI event calendar, no IV-crush model)",
    "regime_scale_* risk multipliers incl. crash:0.0 (engine has no regime-scaled sizing hook)",
    "live compass vix-percentile regime proxy (fleet-wide EXP-3570 gap)",
    "technical trend-filter block"
  ],
  "proxy": {
    "definition": "engine-representable champion CS+IC core of paper_exp401.yaml; identical to EXP-400 twin (max_positions 12 vs 10 never binds; metrics verified equal)",
    "runner": "experiments/honest-fills-fleet/run.py exp401core",
    "window": ["2020-01-02", "2026-04-02"],
    "naive": {"trades": 28, "total_return": -60.97, "cagr": -13.98, "win_rate": 53.57, "sharpe": -0.68, "max_dd": -61.77, "pct_unfillable": 0.0},
    "marketable": {"trades": 83, "total_return": -74.53, "cagr": -19.66, "win_rate": 63.86, "sharpe": -0.48, "max_dd": -80.67, "pct_unfillable": 99.26, "pct_unfillable_basis": "per_scan_slot_foc_rejections / (rejections + filled_trades); trade-count basis +196% due to drawdown-breaker path dependence", "unfilled_entries": 11076, "naive_fallbacks": 0}
  }
}
```

## Implication

EXP-401's +25.2 % live paper quarter cannot be attributed to its differentiators (the straddle overlay never traded; regime scaling was never observed in a non-bull regime live) — its live behavior was the champion core, and the champion core loses badly under both fill models on 2020–2026 real marks. If EXP-401 is to remain a candidate, the priority is not more paper time; it is engine support for its two differentiators so a real twin can say whether they add anything.
